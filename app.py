git clone https://github.com/Wavyen/WollahMontana
git checkout -b # eksempelvis "add-health-endpoint"
# lav en lille ændring i app.py
git add . && git commit -m "feat: tilføj /health endpoint"
git push origin add-health-endpoint
